# CI / Testing Dependencies
# Lightweight subset of requirements.txt for fast GitHub Actions runs.
# Heavy training-only packages (torch, torch-geometric, ultralytics,
# onnx, onnxruntime, gradio) are excluded until Phase 5-8 code is merged
# and must be run in full in that phase's CI job.

numpy==1.26.4
pyyaml>=6.0
pysocialforce==1.1.2
networkx==3.2.1

# Testing & Linting
pytest==8.0.2
ruff==0.3.2
