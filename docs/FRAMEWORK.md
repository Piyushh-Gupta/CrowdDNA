# Framework

The Unified Configuration & Plugin Framework manages lifecycle, dependencies, and configuration for CrowdDNA.

Run validation using:
`python -m training.validate_configuration --dry-run`
