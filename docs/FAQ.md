# Frequently Asked Questions

**Q: Can I use CrowdDNA without Docker?**
A: Yes, you can run the python backend directly using `python -m crowdflow_dna`, though Docker is recommended for production parity.

**Q: Where are the generated simulation plots saved?**
A: All runtime artifacts are output to `experiments/` (which is git-ignored).
