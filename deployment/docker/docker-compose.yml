version: '3.8'
services:
  backend:
    build: 
      context: ../../
      dockerfile: deployment/docker/Dockerfile
    ports:
      - "8000:8000"
  frontend:
    build:
      context: ../../frontend
    ports:
      - "5173:5173"